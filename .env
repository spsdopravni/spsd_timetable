# Environment Configuration
# Zkopíruj tento soubor jako .env a uprav hodnoty

# Force mock data (override automatic dev/prod detection)
# Set to "true" to always use mock data (no API calls)
# Set to "false" to always use real API
# Leave empty or remove to use automatic detection (dev=mock, prod=API)
VITE_USE_MOCK_DATA="true"
